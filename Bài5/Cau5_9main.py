def binary search (lst, value):
left, righs=0, len(lst) - 1
while left < = right:
    mid right: (left + sight) // 2
    if let[mid] == values
        return True
   elif lst[mid] < value:
       left = mid 1
   else:
      right = mid-1
return False
